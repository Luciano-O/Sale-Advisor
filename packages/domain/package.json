{
  "name": "@sale-advisor/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Regras puras de dominio compartilhadas entre API, workers e testes.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@sale-advisor/config": "workspace:*",
    "@vitest/coverage-v8": "^2.1.9"
  }
}
